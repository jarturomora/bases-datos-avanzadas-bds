services:
  # ---------- Config Server Replica Set (3 nodos) ----------
  cfg1:
    image: mongodb/mongodb-community-server:8.0-ubi8
    container_name: cfg1
    hostname: cfg1
    command: >
      mongod --configsvr --replSet cfgRS --port 27019 --bind_ip_all
    ports:
      - "27019:27019"
    volumes:
      - cfg1_data:/data/db
    networks: [mongo-net]

  cfg2:
    image: mongodb/mongodb-community-server:8.0-ubi8
    container_name: cfg2
    hostname: cfg2
    command: >
      mongod --configsvr --replSet cfgRS --port 27019 --bind_ip_all
    volumes:
      - cfg2_data:/data/db
    networks: [mongo-net]

  cfg3:
    image: mongodb/mongodb-community-server:8.0-ubi8
    container_name: cfg3
    hostname: cfg3
    command: >
      mongod --configsvr --replSet cfgRS --port 27019 --bind_ip_all
    volumes:
      - cfg3_data:/data/db
    networks: [mongo-net]

  # ---------- Shards (Replica Set, 1 nodo por shard para aula) ----------
  shard-eu-1:
    image: mongodb/mongodb-community-server:8.0-ubi8
    container_name: shard-eu-1
    hostname: shard-eu-1
    command: >
      mongod --shardsvr --replSet shardEU --port 27018 --bind_ip_all
    ports:
      - "27018:27018"
    volumes:
      - shard_eu_1_data:/data/db
    networks: [mongo-net]

  shard-am-1:
    image: mongodb/mongodb-community-server:8.0-ubi8
    container_name: shard-am-1
    hostname: shard-am-1
    command: >
      mongod --shardsvr --replSet shardAM --port 27020 --bind_ip_all
    ports:
      - "27020:27020"
    volumes:
      - shard_am_1_data:/data/db
    networks: [mongo-net]

  # ---------- Router ----------
  mongos:
    image: mongodb/mongodb-community-server:8.0-ubi8
    container_name: mongos
    hostname: mongos
    depends_on:
      - cfg1
      - cfg2
      - cfg3
    command: >
      mongos --configdb cfgRS/cfg1:27019,cfg2:27019,cfg3:27019 --port 27017 --bind_ip_all
    ports:
      - "27017:27017"
    restart: unless-stopped
    networks: [mongo-net]

  # ---------- Init (orquestador) ----------
  init:
    image: mongodb/mongodb-community-server:8.0-ubi8
    container_name: mongo-init
    depends_on:
      - cfg1
      - cfg2
      - cfg3
      - shard-eu-1
      - shard-am-1
      - mongos
    volumes:
      - ./init:/init:ro
    command: ["/bin/bash", "/init/init.sh"]
    restart: "no"
    networks: [mongo-net]

volumes:
  cfg1_data:
  cfg2_data:
  cfg3_data:
  shard_eu_1_data:
  shard_am_1_data:

networks:
  mongo-net:
    driver: bridge
